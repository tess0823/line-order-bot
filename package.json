{
  "name": "line-order-bot",
  "version": "1.0.0",
  "description": "A simple temporary LINE group ordering bot.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
